import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.envologiaoop.weynebirre',
  appName: 'Weyne-birre',
  webDir: 'dist',
};

export default config;
